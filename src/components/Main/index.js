import React from 'react';
import classes from './main.module.css'

const Main = () => {
    return (
        <main className={classes.MainSection}>
            Header
        </main>
    );
};

export default Main;
